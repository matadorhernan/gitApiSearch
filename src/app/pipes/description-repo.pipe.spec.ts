import { DescriptionRepoPipe } from './description-repo.pipe';

describe('DescriptionRepoPipe', () => {
  it('create an instance', () => {
    const pipe = new DescriptionRepoPipe();
    expect(pipe).toBeTruthy();
  });
});
